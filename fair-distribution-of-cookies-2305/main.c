#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <stdlib.h>


/* cookies is an integer array such that ith element represents the number of
 * cookies in the ith bag and k is the number of children. We need to distribute
 * all the cookie bags. The unfairness of a distribution is defined as the
 * maximum total cookies obtained by a single child.
 *
 * Find the minimum unfairness. */

// Create a tree from it then do something from there i dunno
int distributeCookies(int *cookies, int cookiesSize, int k){
  return 0;
}
